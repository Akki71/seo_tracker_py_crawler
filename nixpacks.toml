[phases.setup]
nixPkgs = [
  "python313",
  "gcc",
  "freetype",
  "libjpeg",
  "zlib",
  "libpq",
  "dejavu_fonts",
  "curl",
]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "pip install -r requirements.txt"
]

[start]
cmd = "python -u startup.py"
